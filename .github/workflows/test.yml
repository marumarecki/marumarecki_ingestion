name: Test
on:
    schedule:
        # Run daily at 3:00 AM UTC
        - cron: '0 3 * * *'
    workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    environment:
      name: ingestion  
      deployment: false
    steps:
      - name: run tests
        env:
          API_KEY: ${{ secrets.R2_BUCKET_NAME }}
        run: echo "Running tests with staging secrets"